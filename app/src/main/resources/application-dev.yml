spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/food-reservation
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        default_batch_fetch_size: 100
logging.level:
  org.hibernate.SQL: debug
  #org.hibernate.type: trace
jasypt:
  encryptor:
    password: ${JEP}
